import { Utils } from '../app/Utils';

describe('Utils test suit', () => {
    test('First test', () => {
        const abc = Utils.toUpperCase('something');
        console.log('working')
    })

});